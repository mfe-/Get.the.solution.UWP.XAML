<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Get.the.solution.UWP.XAML"
    >

    <!--
    Default Menu Item for uwp toolkit menuitem x:DataType="local:MenuItem"
    -->
    <DataTemplate x:Key="DefaultMenuItemTemplate" >
        <Grid Width="240" Height="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <SymbolIcon Grid.Column="0" Symbol="{Binding Path=Icon, Mode=OneWay}" Foreground="White" />
            <TextBlock Grid.Column="1" Text="{Binding Path=Name, Mode=OneWay}" FontSize="16" VerticalAlignment="Center" Foreground="White" />
        </Grid>
    </DataTemplate>

</ResourceDictionary>
